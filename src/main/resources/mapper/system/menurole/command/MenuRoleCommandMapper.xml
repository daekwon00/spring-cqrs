<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.study.springcqrs.system.menurole.mapper.command.MenuRoleCommandMapper">

    <delete id="deleteByRoleId">
        DELETE FROM tb_role_menu WHERE role_id = #{roleId}
    </delete>

    <insert id="insertMenuRole">
        INSERT INTO tb_role_menu (role_id, menu_id, can_read, can_create, can_update, can_delete, created_by)
        VALUES (#{roleId}, #{menuId}, true, true, true, true, #{createdBy})
    </insert>

</mapper>
